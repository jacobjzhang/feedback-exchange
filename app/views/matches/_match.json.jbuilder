json.extract! match, :id, :creator_first, :creator_second, :score_card_from_first, :score_card_from_second, :will_exchange_info_first, :will_exchange_info_second, :created_at, :updated_at
json.url match_url(match, format: :json)
